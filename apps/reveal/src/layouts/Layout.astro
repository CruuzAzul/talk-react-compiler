---
const {title} = Astro.props;
---

<!doctype html>
<html lang="en">
	<head>
		<meta charset="UTF-8"/>
		<meta name="viewport" content="width=device-width"/>
		<link rel="icon" type="image/svg+xml" href="/slides-template/favicon.svg"/>

		<link rel="stylesheet" href="plugin/highlight/monokai.css"/>
		<meta name="generator" content={Astro.generator}/>

		<title>{title}</title>
	</head>

	<body>
		<slot/>

		<script>
			import Reveal from "reveal.js";
			import RevealHighlight from "reveal.js/plugin/highlight/highlight";

			Reveal.initialize({
				transition: "none",
				history: true,
				// progress: false,
				// controls: false,
				autoAnimateDuration: 0.5,
				autoAnimateEasing: "cubic-bezier(0.4, 0, 0.2, 1)",
				plugins: [RevealHighlight], // use this if you want to show some code
			});
		</script>
	</body>
</html>

<style is:global>
	@import "reveal.js/dist/reveal.css";
	@import "reveal.js/dist/theme/white.css";
	@import "../../src/styles/highlightjs-theme-vs2015.css";
	@import url('https://fonts.googleapis.com/css2?family=Montserrat:ital,wght@0,100..900;1,100..900&display=swap');

	:root {
		--primary-color: #0A7EA4;
		--dark-background-color: #0A7EA499;
		--background-color: #E6F7FF;
		--lighter-grey: #F5F5F5;
		--light-grey: #e4e4e4;
		--grey: #999;
		--dark-grey: #23272F;
	}

	*:not(pre):not(pre *) {
		font-family: 'Montserrat', sans-serif !important;
		font-optical-sizing: auto;
		font-style: normal;
	}

	pre {
		border-radius: 10px;
	}

	code {
		font-size: 1.5rem;
		border-radius: 10px;
		padding: 2rem 2rem !important;
		max-height: unset !important;
		border: 2px solid var(--primary-color);
	}

	section.present.present {
		.cm-content {
			&:hover {
				cursor: url("data:image/svg+xml;utf8,<svg xmlns='http://www.w3.org/2000/svg'  width='100' height='100' viewport='0 0 100 100' style='fill:black;font-size:50px;'><text y='50%'>🪄</text></svg>") 16 0, auto !important;
			}
		}
	}
</style>
